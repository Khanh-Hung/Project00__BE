namespace Domain.Enums;

/// <summary>
/// Priority classification for character activities and visual moment generation.
/// </summary>
public enum ActivityPriority
{
    Low = 0,
    Normal = 1,
    High = 2,
    Critical = 3
}
